const config = {
  printWidth: 120,
  singleQuote: true,
  useTabs: false,
  tabWidth: 2,
  semi: true,
  bracketSpacing: true,
  quoteProps: 'as-needed',
  arrowParens: 'avoid',
  htmlWhitespaceSensitivity: 'ignore',
  proseWrap: 'always',
};

// eslint-disable-next-line import/no-default-export -- required by prettier
export default config;
